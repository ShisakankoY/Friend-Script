# Friend-Script

/Japanese/

# ミニインタープリター（JavaScript）

JavaScript で書かれた簡易スクリプト言語用インタープリターです。  
対応コマンド: `set`、`Go`、`If`、`loop`

## 使い方

1. このスクリプトを `friend.js` として保存
2. Node.js で実行:

```bash
node script.js

/English/

# Mini Interpreter (JavaScript)

A simple interpreter for a custom scripting language written in JavaScript.  
Supported commands: `set`, `Go`, `If`, `loop`

## Usage

1. Save this script as `friend.js`
2. Run with Node.js:

```bash
node script.js
